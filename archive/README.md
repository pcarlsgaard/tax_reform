# Historical research scripts

The original one-off notebook patchers and plot runners are retained here as provenance. They used hard-coded local Windows paths and duplicated model logic, so they are not imported by the web app, data builder, or tests.

The original notebooks remain at the repository root unchanged. Active assumptions live in `MODEL_SPEC.md`, versioned JSON under `src/data/`, and pure functions under `src/model/`.
